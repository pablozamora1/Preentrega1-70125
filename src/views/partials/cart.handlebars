<h1>Ecomerce Tech</h1>
<div class="homeCards">

    {{#each products}}
    <div class="cardProducts">
        <img src={{this.thumbnails}} alt="">
        <h3>Titulo: {{this.title}}</h3>
        <p>Precio: {{this.price}}</p>
    </div>
    {{/each}}

</div>